# Copy this file to `.env` and fill in your keys.
# Lines starting with # are comments and will be ignored.
# Both KEY=value and `export KEY=value` formats are supported.

# Google Gemini API key
GOOGLE_API_KEY=your_google_api_key_here

# OpenWeatherMap API key (for weather tool)
OPENWEATHERMAP_API_KEY=your_openweathermap_api_key_here

# Optional: override model (defaults to gemini-flash-latest in code)
# Common values: gemini-flash-latest, gemini-1.5-pro-latest
GEMINI_MODEL=gemini-flash-latest
